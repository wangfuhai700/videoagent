# mock 成片样本

主题：TencentOS 离在线混部。2026-09-05 在 Cursor Cloud Linux 上用 `--provider mock --tts mock` 跑出。

- `final.mp4`：720×1280，约 10.5 秒，3 镜色块 + 口播时间轴字幕
- `storyboard.json`：拆镜结果
- `subtitles.ass`：按音频时长对齐
- `state.json`：步骤记录

画面不是 Seedance/万相生成的，只验证流水线。
